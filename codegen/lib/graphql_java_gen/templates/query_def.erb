<%= class_head %>

<%= render_java_doc("Interface used to determine which parts of a #{name.downcase.gsub("query", "")} to return during API operations.", "@author #{code_author}") %>
public interface <%= name %>Definition {
    <%= render_java_doc(
      "Method called to generate the arguments which should be returned.",
      "@param builder used to define which parts of the #{name.gsub("Query", "").downcase} to return"
    ) %>
    void define(<%= name %> builder);
}
